{
  "name": "challancheck-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "spike:bedrock": "node scripts/bedrock-spike.js",
    "server": "node backend/server.js",
    "check:bedrock-text": "node scripts/bedrock-text-check.js"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@aws-sdk/client-rekognition": "^3.1136.0"
  }
}
